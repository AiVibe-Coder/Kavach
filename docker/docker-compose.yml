services:
  kavach-proxy:
    image: nginx:alpine
    ports:
      - "3848:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
